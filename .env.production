# Defaults for production builds. Anything passed via the shell or a CI env
# overrides what's here (Next.js loads env in this order: process.env > .env.local
# > .env.production > .env). Never put secrets in this file — it is committed.

NEXT_PUBLIC_CONVEX_URL=https://brazen-wren-605.convex.cloud
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_ZGlzdGluY3QtZ29sZGZpc2gtNDguY2xlcmsuYWNjb3VudHMuZGV2JA
